export function UnderlineAccent({ children }) {
  return <span className="underline-accent">{children}</span>;
}

export function MarkerEmphasis({ children }) {
  return <span className="marker-emphasis">{children}</span>;
}

export function BlockQuoteLead({ children }) {
  return <p className="blockquote-lead">{children}</p>;
}

export function GradientStrong({ children }) {
  return <strong className="gradient-strong">{children}</strong>;
}
