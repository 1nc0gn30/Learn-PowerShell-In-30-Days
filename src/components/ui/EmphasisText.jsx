export function StrokeText({ children }) {
  return <span className="text-stroke">{children}</span>;
}

export function ItalicAccent({ children }) {
  return <span className="text-italic-accent">{children}</span>;
}

export function HighlightChip({ children }) {
  return <span className="highlight-chip">{children}</span>;
}
